rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // usuarios logueados pueden leer/escribir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}
