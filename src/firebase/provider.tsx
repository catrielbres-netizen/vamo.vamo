'use client';
// This file is intentionally left blank.
// The provider logic has been moved to client-provider.tsx to enforce client-side execution.
// Keeping the file prevents breaking existing imports, but the new architecture should be used.
export {};
