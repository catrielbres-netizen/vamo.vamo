// This dynamic route page is obsolete and has been removed to simplify the build process.
// The new page is /admin/driver-detail and uses query parameters.
export default function ObsoleteDriverDetailPage() {
  return null;
}
