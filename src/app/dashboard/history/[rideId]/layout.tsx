// This layout is now a neutral wrapper.
// The dynamic rendering is controlled by the page.tsx file.
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
