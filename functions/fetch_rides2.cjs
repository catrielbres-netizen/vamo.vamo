const admin = require('firebase-admin'); const serviceAccount = require('../service-account.json'); if (!admin.apps.length) { admin.initializeApp({ credential: admin.credential.cert(serviceAccount) }); } const db = admin.firestore(); async function checkRides() { try { const snapshot = await db.collection('rides').where('passengerId', '==', 'kLH80TIZmkh4WKuw4rot7O7lxEi1').where('cityKey', '==', 'rio_gallegos').get(); console.log('Found ' + snapshot.docs.length + ' rides.'); } catch (e) { console.error('ERROR:', e); } } checkRides();
